name: sync_datetime
description: A production-quality Dart package for reliable and predictable DateTime synchronization between clients and servers.
version: 0.1.3
repository: https://github.com/rahimuj570/sync_datetime

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
